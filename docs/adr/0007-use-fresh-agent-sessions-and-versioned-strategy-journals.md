# 使用新会话和版本化策略记录

每次候选尝试启动新的 DeepSeek Harness Session，长期进化记忆通过插件仓库 `lineage/` 中的只追加策略计划、不可变版本谱系，以及 SQLite 中由可信编排器保存的结构化结果记录传递。每次尝试只允许智能体新增一份有界策略计划，已完成条目只读；后续会话通过候选身份读取对应可信结果，不把结果复制进插件运行载荷。该选择放弃隐式延续对话及改写历史的便利，以避免上下文压缩、历史长度和未记录会话状态成为不可复现实验变量。
