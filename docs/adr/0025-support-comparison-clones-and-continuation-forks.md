# 区分比较克隆与延续分支

比较克隆复用基线、兼容性指纹、`evaluationSuiteId` 和密封评测套件并建立独立审计链，用于公平比较不同模型；所有克隆共同属于同一密封组，不能独立解封或重新密封。延续分支从选定冠军版本开始，使用新的隐藏主种子、新的 `evaluationSuiteId` 和新的密封组，并允许选择新的冻结配置。两种操作都创建新实验而不修改原实验，从而区分控制变量比较与延续探索。
