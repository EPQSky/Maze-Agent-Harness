# 将可信竞技场与可进化插件隔离

Arena Server 独立拥有协议状态、局部观察生成、完整拓扑、规则验证、资源限制、计分、隐藏评测和不可变比赛记录；两个进化智能体只能修改各自的候选插件工作区。候选插件及加载它的整个 Match Profile 子进程均视为不可信，进程内的协议适配插件不持有隐藏状态或裁判权威。该边界以牺牲部分自修改自由度为代价，防止智能体通过篡改同进程组件、制造无解迷宫或污染证据获得虚假优势，并使自主改进结果可以审计和复现。
