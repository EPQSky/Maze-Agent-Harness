# 对求解器隐藏生成种子

生成器获得 Arena Server 提供的确定性种子并据此产生迷宫，求解器不获得该种子，只能使用入口、出口、局部通路、剩余步数和动作结果。系统不向双方提供时间、随机、网络或外部状态能力，但不声称仅靠容器就能完全禁止 Node.js 进程内的时间和随机 API；正式评测通过跨启动时间和调度扰动的重复执行检测其影响，任何输出差异都关闭失败。该非对称信息边界阻止求解器直接获得或复现生成器种子，使评测继续衡量局部观察下的探索算法。
