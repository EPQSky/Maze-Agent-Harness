# 将代码进化与比赛执行分离

大模型只在进化代中分析公开反馈并修改各自 DeepSeek Harness 插件，正式比赛则在无模型适配器和 Agent Loop 的隔离 Match Profile 中运行固定插件版本。运行环境无网络、受资源限制并由 Arena Server 注入必要输入；整个比赛子进程均视为不可信。该选择放弃比赛期间的大模型临场推理，以换取对插件改进的独立测量、确定性复检和可控运行成本；同一插件版本、规则和种子的输出若在重复运行中不一致，候选版本必须关闭失败。
