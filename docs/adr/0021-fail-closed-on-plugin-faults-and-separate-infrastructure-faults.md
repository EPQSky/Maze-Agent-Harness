# 插件故障关闭失败并区分基础设施故障

插件加载失败、能力注册缺失或冲突、超时、OOM、非法桥接协议、非零退出、非法迷宫或非确定性均使候选失败，不能进入缓存；竞技场、Match Profile 容器或存储故障最多重试两次，仍失败则停止实验且不产生对抗结论。模型提供方限流、断网或服务错误退避重试三次，仍失败则暂停实验且不消耗候选尝试或进化代；模型正常返回无效插件代码仍属于候选失败。该分类拒绝用不完整结果猜测胜负，也避免把平台问题错误归因于插件能力。
