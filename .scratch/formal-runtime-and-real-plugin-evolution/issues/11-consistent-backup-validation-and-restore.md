# 11 — 一致性备份、校验与恢复

**What to build:** 在正式实验开始前和结束后创建可验证的完整备份，同时捕获 SQLite、双方 Git 插件谱系、晋级标签和运行身份清单，并允许操作员验证和恢复最近十份备份。

**Blocked by:** 04 — 正式启动、停止与状态检查

**Status:** done

- [x] 备份操作在一致性边界内捕获数据库、双方仓库、标签对象及恢复所需运行清单，并生成完整性摘要。
- [x] 实验正式启动前和进入终态后自动触发备份；失败时返回清晰状态且不会把不完整产物标记为成功。
- [x] 备份轮转只保留最近十份完整备份，不会因清理失败删除最后一个可用恢复点。
- [x] 校验操作能够检测缺失数据库、缺失仓库、晋级标签漂移、清单冲突和摘要损坏。
- [x] 恢复到隔离目标后，实验事实、候选提交、冠军引用和晋级标签通过现有完整性校验；单独 SQLite 副本被拒绝。
- [x] 备份、清单、日志和恢复输出不包含 API Key；应用不自行实现加密，并明确依赖操作系统保护备份介质。
